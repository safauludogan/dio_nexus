name: dio_nexus
description: NetworkManager for Flutter
version: 0.3.0

homepage: https://github.com/safauludogan/dio_nexus

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Network
  dio: ^5.8.0+1

  # Utility
  retry: ^3.1.2 
  connectivity_plus: ^6.1.3
  freezed: ^2.5.8
  json_serializable: ^6.9.4
  fluttertoast: ^8.2.12
  logger: ^2.5.0

  # Other
  freezed_annotation: ^2.4.4
  flutter_localizations:
    sdk: flutter
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  very_good_analysis: ^7.0.0
  
flutter:
